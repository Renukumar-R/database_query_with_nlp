[
    {'Question' : "How many employees have less than 2 years of prior experience in tesla?",
     'SQLQuery' : "SELECT count(*) FROM employement_details AS e JOIN personal_details AS p ON e.employee_id = p.employee_id WHERE e.company = 'Tesla, Inc.' AND p.prior_years_experience < 2",
     'SQLResult': "Result of the SQL query",
     'Answer' : "412"},
    {'Question' : "How many employees have grater than 30 age in tesla?",
     'SQLQuery' : "SELECT COUNT(*) FROM personal_details AS p JOIN employement_details AS e ON p.employee_id = e.employee_id WHERE e.company = 'Tesla, Inc.' AND p.age > 30",
     'SQLResult': "Result of the SQL query",
     'Answer' : "1003"},
    {'Question' : "How many female employees have grater than 30 age in tesla?",
     'SQLQuery' : "SELECT COUNT(*) FROM personal_details AS p JOIN employement_details AS e ON p.employee_id = e.employee_id WHERE e.company = 'Tesla, Inc.' AND p.sex = 'Female' AND p.age > 30",
     'SQLResult': "Result of the SQL query",
     'Answer' : "399"},
    {'Question' : "How many female employees have grater than 30 age in tesla and in AI department?",
     'SQLQuery' : "SELECT count(*) FROM employement_details AS e JOIN personal_details AS p ON e.employee_id = p.employee_id WHERE e.company = 'Tesla, Inc.' AND e.department = 'AI' AND p.sex = 'Female' AND p.age > 30",
     'SQLResult': "Result of the SQL query",
     'Answer' : "47"},
    {'Question' : "How many employees have grater than 30 age in AI or big data department?",
     'SQLQuery' : "SELECT COUNT(*) FROM employement_details AS e JOIN personal_details AS p ON e.employee_id = p.employee_id WHERE e.department IN ('AI', 'BigData') AND p.age > 30",
     'SQLResult': "Result of the SQL query",
     'Answer' : "1620"},
    {'Question' : "which company have higher prior average experience employees?",
     'SQLQuery' : "SELECT company, AVG(prior_years_experience) AS avg_prior_years_experience FROM employement_details AS e JOIN personal_details AS p ON e.employee_id = p.employee_id GROUP BY company ORDER BY avg_prior_years_experience DESC LIMIT 1",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Google LLC."},
    {'Question' : "how many employees have higher prior experience then average prior experience in tesla?",
     'SQLQuery' : "SELECT count(*) FROM employement_details AS e JOIN personal_details AS p ON e.employee_id = p.employee_id WHERE e.company = 'Tesla, Inc.' AND p.prior_years_experience > (SELECT avg(prior_years_experience) FROM personal_details AS p JOIN employement_details AS e ON e.employee_id = p.employee_id WHERE e.company = 'Tesla, Inc.')",
     'SQLResult': "Result of the SQL query",
     'Answer' : "444"},
    {'Question' : "which company have lower average prior experience then overall average prior experience?",
     'SQLQuery' : "SELECT company FROM employement_details AS e JOIN personal_details AS p ON e.employee_id = p.employee_id GROUP BY company ORDER BY AVG(prior_years_experience) ASC LIMIT 1",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Tesla, Inc."},
    {'Question' : "list the top three employees name with salary that high pay?",
     'SQLQuery' : "SELECT name, salary FROM personal_details AS p JOIN employement_details AS e ON p.employee_id = e.employee_id ORDER BY salary DESC LIMIT 3",
     'SQLResult': "Result of the SQL query",
     'Answer' : "Aarif - 153000.0 Aman Kumar - 153000.0 Amar - 153000.0"}
]